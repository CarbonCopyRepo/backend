//Reference: https://jest-extended.jestcommunity.dev/docs/getting-started/setup
import * as matchers from "jest-extended";

// Currently extending expect with all additional matchers
expect.extend(matchers);
